---
const { path, title, blank } = Astro.props;
---

<a
  href={path}
  title={title}
  target={blank ? '_blank' : '_self'}
  class='flex aspect-square items-center justify-center bg-black/75 p-2 text-center text-sm text-white duration-300 hover:bg-black/50 lg:text-base xl:text-lg 2xl:text-xl'
  >{title}</a
>

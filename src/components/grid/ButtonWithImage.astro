---
const { path, alt, title, src, className = '' } = Astro.props;
---

<a
  href={path}
  title={title}
  class={'flex aspect-square items-center justify-center bg-black/75 p-2 text-center text-sm duration-300 hover:bg-black/50 lg:text-base xl:text-lg 2xl:text-xl ' +
    className}><img src={src} alt={alt} title={title} width='200' height='200' /></a
>
